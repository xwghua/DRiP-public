- 每一个控件都是handles这个结构体下的元素，每一个控件中比较重要的、编程中会用到的属性有Tag, Value, String, 有的还会包括Min，Max等等。

- 从例子中可以看到，我们一般都会在程序的开头加上全局变量的申明以及load变量的函数。而在“\*\*_OpenFunc(...)”函数（\*\*为你的文件名）中我们将按照自己需要的顺序加上对这些函数的调用。“\*\*_OpenFunc(...)”函数为预加载函数，是在程序最开始运行的函数。运行完这个函数后，接下来我们每次对界面的操作都表现在程序对每个控件的回调函数的运行。我们之后所有的编程也将都在\*\*_Callback(...)函数（\*\*为控件的Tag）中实现。

- 从两个例子中，可以看出以上所述内容。不过值得注意的是，我们可以使每次对控件的回调实时地表现出来（比如通过imshow表达），也可以在对控件回调后通过button控件重新load之前的操作（即点击按钮才会运行程序，非实时变化）。这其中的区别在于，前者你需要在控件的回调函数中全都加上诸如imshow，plot之类的显示函数，而后者则是将

- 这些函数统一都包含在button控件的回调函数中。

---

- 最后，由于实验室师兄没有把完整的程序给我，所以他的那段程序是不可运行的:(。。。我的这段是可以运行的，而且我在写完之后运行了“mcc -m mask_GUI.m” 来产生exe可执行文件。enjoy！

--Shawn